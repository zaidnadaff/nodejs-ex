const open_file = require("./files"); // remember to import as an object or as a function
open_file();

// when importing a module, nodejs does this where it runs the module script while imoprting
// so to prevent any double executions, remember to make sure the module script works as Is.
